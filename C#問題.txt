1.斐波那契數列
public int Foo（int N）
{
if(N<=0)
{
return 0;
}
else if(N>0 && N <=2)
{
return 1;
}
else
{
return Foo(N-1)+Foo(N-2)
}
}

static void main()
{
Foo(30);
}

------------------------------------------------
2.用一用SQL語句 查找出每門功課都大于80分的學生姓名

select 姓名 from table group by 姓名 having min(分數)>80

-------------------------------------------------
3.可以采用Foreach迭代的類的對象必須滿足什?條件
需要實現IEnumerable接口或聲明GetEnumerator方法的類型

--------------------------------------------------
4.Foreach和for循環誰的效率高？Switch和if？
foreach 和switch 
實現IEnumerable接口，調用GetEnumerable方法傳遞對象，以後每次執行只需調用MoveNext。

--------------------------------------------------
5.讀寫數據庫需要哪些對象？簡要?明作用
connection：數據庫連接
command：執行sql命令
dataadapter：對數據源執行操作，返回結果存入dataset
datareader：讀取數據源數據
dataset:?存中的數據庫，可存放任意個datatable

--------------------------------------------------
6.string和stringbuilder的區別
string是不可變的，stringbuilder可以修改，remove，append

--------------------------------------------------
7.int？和int的區別
int?為可空類型，可以是Null，通過將int裝箱為引用類型實現的

--------------------------------------------------
8.描述一下virtual，sealed，override，abstract的區別
virtual申明虛方法的關鍵詞，?明此方法可被重寫
sealed密封方法不可重寫，密封類不可繼承
override子類重寫方法關鍵詞
abstract抽象類無法實例化，只能被子類繼承

--------------------------------------------------
9.List<Int> listNumber，其中存放了從1到100的數字，需要將其中數字從大到小排列輸出。
foreach(int a in listNumber.sort())
{
cosole.writeline(a);
}

--------------------------------------------------
10.int,string,hashtable,datatime,datatable哪些是引用類型
string hashtable，datatable

--------------------------------------------------
11.簡述public，private，protected，internal修飾符的訪問權限
public完全公開，無限制
private此類中訪問
protected此類和子類中使用
internal該程序集中使用

--------------------------------------------------
12.asp.net頁面之間傳值得幾種方式
request.querystring("").tostring()
request.form[""].tostring()
cookie,session,application

--------------------------------------------------
13.C#委托是什?？事件是不是委托？
委托實際上相當於函數指針，一種函數的引用。委托和事件沒有可比性，委托是類型，事件是對象，事件通過委托實現

--------------------------------------------------
14.什?是裝箱和拆箱？
裝箱是值類型轉化為引用類型，拆箱是引用類型轉化為值類型

--------------------------------------------------
15.值類型和引用類型的區別

1.值類型的數據存儲在棧中，引用類型的數據存儲在堆中，對象存儲在棧中
2.值類型存儲速度快，引用類型慢
3.值類型表示實際數據，引用類型表示存儲在隊中數據的地址
4.棧的?存自動釋放，堆的?容由GC釋放

--------------------------------------------------
16.接口和類的異同
1.接口不能實例化
2.接口不包含方法的實現
3.接口可以多繼承，類只能單繼承

1.都可以被繼承
2.都可以包含事件，屬性，方法

--------------------------------------------------
17.try{}裏有一個return，finally{}的code會不會執行，什?時間實行？
會執行，在return之前執行

--------------------------------------------------
18.進程和?程的區別
1.進程有自己獨立的地址空間，?程共享進程地址空間
2.一個程序至少有一個進程，一個進程至少有一個?程
3.?程是處理器調度的基本單位，進程不是
4.均可并發執行

--------------------------------------------------
19.什?是反射
動態獲取程序集信息，動態獲取類的屬性和方法

--------------------------------------------------
20.什?是匿名方法
delegate（int i）{return i}

--------------------------------------------------
21.行轉列
select * from table as P pivot (sum(行轉列后列的值) for P.需要行轉列的列 in（）) AS　T

--------------------------------------------------
22.列轉行
SELECT *
FROM
(
    SELECT *
     FROM table 
)T
UNPIVOT
(
    列轉行后列值 FOR 列傳行后列名 IN
    (需要列轉行的列名)
) P

--------------------------------------------------
23.描述new的兩種用法
新建對象
覆蓋父類的方法

--------------------------------------------------
24.屬性定義
public string name{

get{
return name;
}
set
{
name=value;
}
}
或
public string name {get;set;}

--------------------------------------------------
25.C#中所有對象共同的基類是什?
System.Object

--------------------------------------------------
26.可以複寫私有的虛方法嗎？
不可以，虛方法不可以用private和static修飾

--------------------------------------------------
27.接口interface是什?？
只包含抽象方法，子類必須全部實現其中方法

--------------------------------------------------
28.什?是成員變量？什?是局部變量？
類範圍?定義的是成員變量，也叫屬性
方法中定義的變量是局部變量

--------------------------------------------------
29.請詳述class和struct的異同
都可以實例
都包含方法屬性成員

class是引用類型，struct是值類型

--------------------------------------------------
30.base關鍵字的作用是什?？
子類中訪問父類同名成員

--------------------------------------------------
31.string str=null ,string str=string.empty,string str="",請?明其中的區別
string.empty不分配?存空間，""分類長度為空的?存空間

--------------------------------------------------
32.三層架構
UI 可視化層
BLL 邏輯處理層
DAL 數據訪問層
之間通過數據模型進行數據交互

--------------------------------------------------
33.是否可以繼承string類？
不可以，string類是sealed不可繼承

--------------------------------------------------
34.如何把array複製但arraylist中？
string []a ={"11","2222"} arraylist b= new arraylist(a)

--------------------------------------------------
35.編寫一個單例類singleTon
public filemanage（）
{
private filemanage（）{}
public static filemanage instance= new filemanage()
}

--------------------------------------------------
36.ref和out區別
ref需要初始化，out必須在方法中初始化
ref用在需要修改傳入參數時，out適合有多個返回值

--------------------------------------------------
37.子類對父類中的虛方法的處理有重寫override和覆蓋new，請?明他們的區別
有父類parentClass 和子類childclass 及虛方法 virtualfunction（）
則parentclass p = new childclass()
p.virtualfunction（）
如果是重寫override則調用子類的方法，如果是覆蓋調用弗雷

--------------------------------------------------
38.寫一段SQL，取得表A第31到第40記錄
select top 10 * from A where id  not in (select top 30 id from A)

--------------------------------------------------
39.所有可序列化的類都被標記為
【serializable】

--------------------------------------------------
40.public const static int a=1;
const 不能被修飾為static，因?定義為const已經是static

--------------------------------------------------
41.遍歷所有控件,找出textbox並清空其?容
foreach (Control ct in this.Controls)
{
if(ct.GetType().tostring()=="textbox")
{
textbox tb = (textbox)ct;
tb.text="";
}

}